import "@/app.less";

import { Button } from "antd";

function App() {
  return (
    <div className="app">
      hello, world;
      <Button
        type="primary"
        size="small"
      >点击
      </Button>
    </div>
  );
}

export default App;
